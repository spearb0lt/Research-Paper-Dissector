# Normalise line endings in the repository, so a checkout on Windows and one on
# Linux produce the same blobs and a diff never turns into every line changed.
* text=auto eol=lf

# Binary, never touched.
*.png binary
*.gif binary
*.webm binary
*.pdf binary
*.onnx binary
